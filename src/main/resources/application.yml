spring:
  application:
    name: Agate
  datasource:
    url: jdbc:postgresql://localhost:5432/agate
    username: youruser
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
    port: 8080